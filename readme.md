## Objetivo

Criar um carrinho de comprar baseado no carrinho de compras da shoppe, aonde o carrinho armazene itens e faça o cálculo de sub-itens automaticamente.

// Domínio da aplicação: carrinho de compras

// as entidades representadas
// -> carrinho = cart.js
// -> itens = item.js